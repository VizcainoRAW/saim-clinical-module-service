##Spring Configuration
server:
  port: 8080
spring:
  application:
    name: saim-clinical-module-service
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: /h2
  profiles:
    include:
